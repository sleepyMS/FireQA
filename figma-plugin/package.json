{
  "name": "fireqa-figjam-plugin",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "FIREQA_SERVER_URL=http://localhost:3000 node build.mjs --watch",
    "build": "node build.mjs",
    "build:dev": "FIREQA_SERVER_URL=http://localhost:3000 node build.mjs",
    "watch": "FIREQA_SERVER_URL=http://localhost:3000 node build.mjs --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.104.0",
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "@types/dagre": "^0.7.54",
    "dagre": "^0.8.5",
    "elkjs": "^0.11.1"
  }
}
